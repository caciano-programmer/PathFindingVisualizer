// TODO implement dijkstra's, BFS, DFS, A*, bellman-ford, bidirectional
// TODO optional best-first-search, D*

type graph = number[][];

const buildGraph = (rows: number, columns: number): graph => {
  const matrix: graph = [];
  return matrix;
};

export const dijkstra = (graph: graph): number[] => {
  const shortestPath: number[] = [];
  return shortestPath;
};
